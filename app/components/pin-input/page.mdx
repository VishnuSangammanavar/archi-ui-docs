import { Tabs, Table } from 'nextra/components'
import PinInputField from '../../../src/demo/pinInput/PinInputField.jsx'
import PinInputVariant from '../../../src/demo/pinInput/PinInputVariant.jsx'
import PinInputSize from '../../../src/demo/pinInput/PinInputSize.jsx'
import PinInputControlled from '../../../src/demo/pinInput/PinInputControlled.jsx'
import PinInputValidation from '../../../src/demo/pinInput/PinInputValidation.jsx'

# PinInput

Used to capture a pin code or otp from the user.
## Basic PinInput

<Tabs items={['Preview', 'code']}>
  <Tabs.Tab value='Preview'>
    <PinInputField />
  </Tabs.Tab>
  <Tabs.Tab value='Code'>
    ```jsx showLineNumbers
    import React from 'react'
    import { PinInput } from 'archi-ui'

    export default function MyReactPage() {
      return (
        <div className="flex items-center justify-center">
          <PinInput length={4} />
        </div>
      );
    }
    ```
  </Tabs.Tab>
</Tabs>

## Usage
```jsx
import { PinInput } from 'archi-ui'
```
```jsx
<Input type="text" placeholder='Enter name' />
```

## Variants
The `PinInput` comes with three variants: outlined(default), filled, standard.

<Tabs items={['Preview', 'code']}>
  <Tabs.Tab value='Preview'>
    <PinInputVariant />
  </Tabs.Tab>
  <Tabs.Tab value='Code'>
    ```jsx showLineNumbers
    import React from 'react';
    import { PinInput } from 'archi-ui'

    export default function MyReactPage() {
      return (
        <div className='flex flex-col items-center justify-center gap-2'>
          <PinInput length={4} variant='outlined' value={'1234'} />
          <PinInput length={4} variant='filled' value={'1234'} />
          <PinInput length={4} variant='standard' value={'1234'} />
        </div>
      );
    }
    ```
  </Tabs.Tab>
</Tabs>

## Size
For different size of Input like small, medium, large use `size` prop.

<Tabs items={['Preview', 'code']}>
  <Tabs.Tab value='Preview'>
    <PinInputSize />
  </Tabs.Tab>
  <Tabs.Tab value='Code'>
    ```jsx showLineNumbers
    import React from 'react';
    import { PinInput } from 'archi-ui'

    export default function MyReactPage() {
      return (
        <div className='flex flex-col flex-wrap items-center justify-center gap-2'>
          <PinInput variant="filled" placeholder="Small" size='sm' />
          <PinInput variant="filled" placeholder="Medium" size='md' />
        </div>
      );
    }
    ```
  </Tabs.Tab>
</Tabs>

## Controlled PinInput
Used to capture a pin code or otp from the user.

<Tabs items={['Preview', 'code']}>
  <Tabs.Tab value='Preview'>
    <PinInputControlled />
  </Tabs.Tab>
  <Tabs.Tab value='Code'>
    ```jsx showLineNumbers {10-12, 19}
    "use client"

    import React, { useState } from 'react'
    import { PinInput } from 'archi-ui'

    export default function MyReactPage() {

      const [pinValue, setPinValue] = useState('');

      const handlePinChange = (value) => {
        setPinValue(value);
      };

      return (
        <div className='flex items-center justify-center'>
          <PinInput
            length={4}
            value={pinValue}
            onChange={handlePinChange}
          />
        </div>
      );
    }
    ```
  </Tabs.Tab>
</Tabs>

## PinInput Validation
The `error` prop toggles the error state. The `FormHelperText` can then be used to provide feedback to the user about the error.

<Tabs items={['Preview', 'code']}>
  <Tabs.Tab value='Preview'>
    <PinInputValidation />
  </Tabs.Tab>
  <Tabs.Tab value='Code'>
    ```jsx showLineNumbers {29}
    "use client"

    import React, { useState } from 'react'
    import { FormControl, FormHelperText, PinInput } from 'archi-ui'

    export default function MyReactPage() {

      const [pinValue, setPinValue] = useState('');
      const [isError, setIsError] = useState(false);
      const [isSuccess, setIsSuccess] = useState(false);

      const handlePinChange = (value) => {
        setPinValue(value);
        setIsError(value !== '' && value !== '1234');
        setIsSuccess(value !== '' && value == '1234');
      };

      return (
        <div className='flex flex-col items-center justify-center gap-2'>
          <p className="mt-2 text-sm">Try typing "1234" to clear the error.</p>
          <FormControl>
            <PinInput
              length={4}
              value={pinValue}
              onChange={handlePinChange}
              error={isError}
              color={isError ? 'error' : 'primary'}
            />
            <FormHelperText error={isError} className={`${isSuccess && 'text-green-500'}`}>{isError ? 'Invalid PIN!' : isSuccess ? 'Success!' : 'Enter the pin'}</FormHelperText>
          </FormControl>
        </div>
      );
    }
    ```
  </Tabs.Tab>
</Tabs>

